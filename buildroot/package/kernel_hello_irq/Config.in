config BR2_PACKAGE_KERNEL_HELLO_IRQ

        bool "kernel_hello_irq"
        help
          This is a kernel model? which start timer, and handles it with irq.

          https://github.com/Ultrafeel/kernel_hello_irq
